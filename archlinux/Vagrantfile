Vagrant.configure("2") do |config|
  config.vm.box = "archlinux/archlinux"
  config.vm.define "node1" do |centos_on_vbox|
    centos_on_vbox.vm.hostname = "node1"
    centos_on_vbox.vm.provider "virtualbox" do |spec|
      spec.memory = 2048
      spec.cpus = 1
    end
  end
end
